# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com
version: 2
env:
  - CGO_ENABLED=0
before:
  hooks:
    - go mod tidy
builds:
  - id: xdperf
    binary: xdperf
    main: ./cmd/xdperf
    goos: [ linux ]
    goarch: [ amd64 ]
  - id: plugin-simpleudp.tinygo
    dir: ./plugins/simpleudp.tinygo
    main: .
    tool: tinygo
    binary: simpleudp.tinygo.wasm
    ldflags:
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
    asmflags: []
    targets:
      - linux_amd64
    flags:
      - -scheduler=none
      - -target=wasip1
      - -buildmode=c-shared
  - id: plugin-simpleudp.go
    dir: ./plugins/simpleudp.go
    main: .
    binary: simpleudp.go
    goos: [ wasip1 ]
    goarch: [ wasm ]
    buildmode: c-shared
    ldflags:
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
  - id: plugin-test_e2e_variety.go
    dir: ./plugins/test_e2e_variety.go
    main: .
    binary: test_e2e_variety.go
    goos: [ wasip1 ]
    goarch: [ wasm ]
    buildmode: c-shared
    ldflags:
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
  - id: plugin-imixudp.go
    dir: ./plugins/imixudp.go
    main: .
    binary: imixudp.go
    goos: [ wasip1 ]
    goarch: [ wasm ]
    buildmode: c-shared
    ldflags:
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
archives:
  - id: xdperf
    formats:
    - 'binary'
    ids:
    - xdperf
  - id: plugin-simpleudp.tinygo
    formats:
    - 'binary'
    ids:
    - plugin-simpleudp.tinygo
  - id: plugin-simpleudp.go
    formats:
    - 'binary'
    ids:
    - plugin-simpleudp.go
  - id: plugin-test_e2e_variety.go
    formats:
    - 'binary'
    ids:
    - plugin-test_e2e_variety.go
  - id: plugin-imixudp.go
    formats:
    - 'binary'
    ids:
    - plugin-imixudp.go
checksum:
  name_template: 'checksums.txt'
snapshot:
  version_template: "{{ incpatch .Version }}-{{ replace .Branch \"/\" \"-\" }}-next"
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
release:
  github:
    owner: takehaya
    name: xdperf
  draft: false
  disable: false
  prerelease: auto
    # this name template makes the OS and Arch compatible with the results of uname.
    # name_template: >-
    #   {{ .ProjectName }}_
    #   {{- title .Os }}_
    #   {{- if eq .Arch "amd64" }}x86_64
    #   {{- else if eq .Arch "386" }}i386
    #   {{- else }}{{ .Arch }}{{ end }}
    #   {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
